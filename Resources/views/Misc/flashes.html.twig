{% for type, messages in app.session.flashbag.all() %}
    <div class="alert alert-{{ type }}">
      <a class="close" data-dismiss="alert">×</a>
        {% for message in messages %}
            {{ message|trans({}, "messages", "fr") }}
        {% endfor %}
    </div>
{% endfor %}
